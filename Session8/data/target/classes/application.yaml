#database_username :
#  postgres
#database_url:
#  jdbc:postgresql://localhost:5432/sessions
#database_password:
#  hggi


spring:
  application:
    name: spring-data
  datasource:
    url: jdbc:postgresql://localhost:5432/sessions
    password: hggi
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  jackson:
    serialization:
      fail-on-empty-beans: false
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379



